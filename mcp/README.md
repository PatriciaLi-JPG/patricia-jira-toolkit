# MCP Notes

This folder is for MCP setup notes and sample config only.

Do not commit real credentials or machine-specific absolute paths. Each user should configure their local Jira, Google Drive, or other MCP access according to company policy.

Suggested use:

- Put example connector configuration in `mcp/configs/*.example.json`.
- Document required permissions in this file.
- Keep local active config out of Git.

